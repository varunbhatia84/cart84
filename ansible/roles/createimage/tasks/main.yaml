---
- name: Stop the VM
  ec2_instance:
    state: stopped
    instance_ids:
      - "{{ webinstanceid }}"
- name: create a AMI
  ec2_ami:
    instance_id: "{{ webinstanceid }}"
    wait: yes
    name: "webapp-{{ versionid }}"
    tags:
      Name: "webapp-{{ versionid }}"
